apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idxworker.fullname" . }}-test-connection"
  labels:
    {{- include "idxworker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idxworker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
